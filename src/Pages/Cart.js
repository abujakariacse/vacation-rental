import React from 'react';

const Cart = () => {
    // const cartStringified = localStorage.getItem('cart');
    // const cart = JSON.parse(cartStringified);
    // const { name, phone, room, quantity, adult, child, checkIn, checkOut, Totaldays, time } = cart;
    return (
        <div>
            <h2 className='text-4xl text-center'>This is Cart</h2>
        </div>
    );
};

export default Cart;